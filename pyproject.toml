[project]
name = "dsm-deals-hub"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi==0.111.0",
  "uvicorn==0.30.1",
  "SQLAlchemy==2.0.30",
  "pydantic==2.7.1",
  "psycopg[binary]>=3.1,<4"
]

[tool.vercel]
entrypoint = "server:app"
